# Keep installs deterministic and quieter across macOS / Windows.
legacy-peer-deps=true
audit=false
fund=false
progress=false

# Prefer the mirror already used by the lockfiles to reduce Windows install stalls.
registry=https://registry.npmmirror.com/
electron_mirror=https://npmmirror.com/mirrors/electron/
